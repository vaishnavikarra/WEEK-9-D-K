apiVersion: v1
kind: Service
metadata:
  name: kubedemoapp-service
spec:
  selector:
    app: kubedemoapp
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30080
  type: NodePort